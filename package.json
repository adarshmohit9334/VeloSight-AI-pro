{
  "name": "velosight-ai-root",
  "version": "1.0.0",
  "private": true,
  "description": "VeloSight AI — Intelligent Traffic Management & Analytics Platform Monorepo",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
